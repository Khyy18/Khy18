"""Compat-shim для импорта `strategy`.

Публичный API перенесён в `strategy_v2` (Donchian + трендовый фильтр +
vol-targeting). Этот модуль оставлен как тонкая обёртка, чтобы старые
импорты `import strategy` продолжали работать без правок.

Стратегия v1 (EMA/RSI-пересечение) по-прежнему доступна как отдельный
модуль: `import strategy_v1` - бэктестер использует его для сравнения.
"""

from strategy_v2 import *  # noqa: F401,F403
